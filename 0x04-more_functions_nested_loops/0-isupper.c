#include "main.h"

/**
 * _isupper - Checks if a letter is uppercase
 * @t: The letter is to be checked
 * Return : returns 1 for uppercase and 0 for lowercase
 */

int _isupper(int t)
{
	if(t >= 65 && t <= 90)
	{
	return (1);
	}
        return (0);
}
